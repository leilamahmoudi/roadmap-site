# Phase 4: Implement

Start a fresh Claude Code session for implementation.

Provide the relevant project files, then ask Claude to read them before writing any code:

1. `SPEC.md`
2. `ARCHITECTURE.md`
3. `task-list.md`

Claude should treat these files as the source of truth for the implementation and follow the tasks in order.

## Example Prompt

```
Read the provided `SPEC.md`, `ARCHITECTURE.md`, and `task-list.md` before making any changes.

Use them as the source of truth for this project.

Start with the first unfinished task in `task-list.md`. Before coding, briefly explain what you are going to implement and which parts of the spec and architecture it depends on.

Implement one task at a time, verify the result, then update `task-list.md` before moving on.

Do not change agreed product or architecture decisions without calling it out first.
```
